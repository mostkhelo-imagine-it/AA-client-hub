# Copy this file to .env and fill in real values.
# .env is git-ignored on purpose — never commit real credentials.

DB_HOST=localhost
DB_NAME=aa_clienthub
DB_USER=aa_clienthub_user
DB_PASS=changeme
DB_CHARSET=utf8mb4

APP_ENV=production
APP_URL=https://hub.example.com

# Shared secret the FluentCommunity/FluentCart webhook must send back
# (Phase 5 — course sync). Leave blank until that's wired up.
WEBHOOK_SECRET=
